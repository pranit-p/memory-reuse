## What does this PR do?

A short summary of the change.

## Why?

The motivation or the issue it fixes (link the issue: `Fixes #123`).

## How was it tested?

- [ ] Added/updated unit tests
- [ ] `pytest tests/` passes
- [ ] `ruff check memory_reuse tests` passes
- [ ] `black --check memory_reuse tests` passes

## Checklist

- [ ] Public classes/functions have docstrings
- [ ] Type hints added
- [ ] No secrets or cached values are logged
- [ ] CHANGELOG updated under `[Unreleased]`
