# Backends

## AbstractBackend

::: memory_reuse.backends.base.AbstractBackend

## InMemoryBackend

::: memory_reuse.backends.memory.InMemoryBackend

## RedisBackend

Requires the `redis` extra (`pip install "memory-reuse[redis]"`).

::: memory_reuse.backends.redis.RedisBackend

## AgentCoreBackend

Requires the `agentcore` extra (`pip install "memory-reuse[agentcore]"`).

::: memory_reuse.backends.agentcore.AgentCoreSettings

::: memory_reuse.backends.agentcore.AgentCoreBackend
