# Integrations

## LangGraph

::: memory_reuse.integrations.langgraph.cached_node

::: memory_reuse.integrations.langgraph.cached_tool

## LiteLLM

::: memory_reuse.integrations.litellm.cached_litellm_completion

::: memory_reuse.integrations.litellm.cached_litellm_embedding

## Strands Agents

Requires the `strands` extra (`pip install "memory-reuse[strands]"`).

::: memory_reuse.integrations.strands.cached_tool

## CrewAI

Requires the `crewai` extra (`pip install "memory-reuse[crewai]"`).

::: memory_reuse.integrations.crewai.cached_tool
